import { DEFAULT_UPSET_VARIANCE } from '@shared/engine/matchSimulator.js';

export { DEFAULT_UPSET_VARIANCE };

export const UPSET_VARIANCE_MAX = 1;

export const UPSET_FACTOR_HINT =
  'Higher values add more random form swings — title favourites drop points, strugglers pull off shocks. 0 turns upsets off.';
